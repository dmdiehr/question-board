{{#if userName.isAdmin}}
  {{#if editQuestionForm}}
  <div>
    <form>
      <div class="form-group">
        {{input value=question.title}}
      </div>
      <div class="form-group">
        {{textarea class="big" value=question.body}}
      </div>
      <button {{action 'editQuestion' question}}>Upgrayedd</button>
      <button {{action 'hideEditForm'}}>NVM</button>
      <br>
    </form>
  </div>
  {{else}}
      <button {{action 'editQuestionForm'}}>This is kinda stupid</button>
      <button {{action 'deleteQuestion' question}}>This is REALLY stupid</button><br><br>
  {{/if}}
{{/if}}
